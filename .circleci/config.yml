version: 2.1
jobs:
  build_docker:
    machine: true

    steps:
      - run: echo "directory is $PWD"
      - run: ls
      - run: docker build -t personal/website:$CIRCLE_SHA1 .

workflows:
  buildDocker:
    jobs:
      - build_docker
